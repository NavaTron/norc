//! TLS transport implementation

/// TLS transport module (placeholder for future implementation)
pub struct TlsTransport;

impl TlsTransport {
    /// Create a new TLS transport
    pub fn new() -> Self {
        Self
    }
}

impl Default for TlsTransport {
    fn default() -> Self {
        Self::new()
    }
}
